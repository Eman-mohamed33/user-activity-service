apiVersion: v1
kind: Secret
metadata:
  name: user-activity-secrets
  namespace: user-activity
type: Opaque
stringData:
  # Replace this only if MongoDB authentication is enabled.
  MONGO_USERNAME: example
  MONGO_PASSWORD: change-me
